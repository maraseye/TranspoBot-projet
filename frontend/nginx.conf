server {
    listen 80;
    charset utf-8;
    root /usr/share/nginx/html;
    index index.html;

    # Toutes les routes React (SPA) → index.html
    # Le routing /api/* est géré par Traefik en amont, pas par ce Nginx
    location / {
        try_files $uri $uri/ /index.html;
    }
}
